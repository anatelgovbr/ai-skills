# <Assunto>

<Uma frase declarando sobre o que este arquivo tem autoridade.>

**Consulte quando:** <gatilho objetivo, no momento em que o agente precisa disto>

## <Secao mais consultada>

<Conhecimento, do mais consultado para o menos. Tabela quando o conteudo e comparativo; prosa quando e sequencial. Exemplo de codigo somente quando o padrao nao se descreve em palavras.>

## <Secao seguinte>

<...>

## Exemplares

<Obrigatorio quando a reference descreve uma unidade do sistema. O corpo acima ja precisa bastar
para implementar sem abrir nenhum destes arquivos: o exemplar confirma a forma local do modulo,
nao substitui a descricao do padrao.>

| Caminho | O que ilustra | Como localizar o equivalente hoje |
|---|---|---|
| `<caminho completo>` | <caso tipico, caso antigo ou excecao nomeada> | `<comando ou regra de nomenclatura>` |

## Evidencias

| Afirmacao | Onde foi comprovada | Comando | Alcance | Verificado em |
|---|---|---|---|---|
| <resumo da afirmacao> | `<caminho:linha>`, `<caminho:linha>` (`<N>` ocorrencias em `<M>` arquivos) | `<busca literal, com o escopo>` | `<alcance>` | `<data>` |
